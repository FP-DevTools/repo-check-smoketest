name: Compliance

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  compliance:
    uses: FP-DevTools/repo-standard-kit/.github/workflows/compliance.yml@fix/compliance-workflow-ref-resolution
    with:
      ref: fix/compliance-workflow-ref-resolution
