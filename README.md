# repo-check-smoketest

Scratch repository used to verify that `repo-standard-kit`'s
`.github/workflows/compliance.yml` reusable workflow can be called from
another repository via `workflow_call`. Safe to delete once the verification
run has been observed.
